query GetCharacterById($id: ID!) {
  character(id: $id) {
    id
    name
    status
    species
    type
    gender
    image
    origin {
      name
      dimension
    }
    location {
      name
      dimension
    }
    episode {
      id
      name
      episode
    }
  }
}
